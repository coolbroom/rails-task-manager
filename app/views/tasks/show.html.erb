<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<p>This link is not completed </p>


<%= link_to "Back to list", tasks_path  %><br>
<%= link_to "Edit the task", edit_path  %><br>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
